From 2cead2b184287771a95578761dc6cb413d36a67e Mon Sep 17 00:00:00 2001
From: Shadow Coders <shadowcoders@users.noreply.github.com>
Date: Thu, 17 Sep 2026 21:14:26 +0200
Subject: [PATCH] SMOUK 0.0.44 avoid saving layout during dock relayout

---
 src/windows/main_window.py     | 8 ++++++--
 src/windows/verticalization.py | 5 -----
 2 files changed, 6 insertions(+), 7 deletions(-)

diff --git a/src/windows/main_window.py b/src/windows/main_window.py
index 433492f93..8a724c518 100644
--- a/src/windows/main_window.py
+++ b/src/windows/main_window.py
@@ -102,7 +102,7 @@ from windows.views.tutorial import TutorialManager
 
 # SMOUK application version. Increment this patch number for every SMOUK UI
 # or workflow change released from this codebase.
-SMOUK_VERSION = "0.0.43"
+SMOUK_VERSION = "0.0.44"
 
 
 class MainWindow(updates.UpdateWatcher, QMainWindow):
@@ -4410,7 +4410,11 @@ class MainWindow(updates.UpdateWatcher, QMainWindow):
         QMainWindow.resizeEvent(self, event)
         if self.tutorial_manager:
             self.tutorial_manager.re_position_dialog()
-        self._schedule_layout_checkpoint()
+        # A dock's expanding child widgets can cause several resize events
+        # while Qt is still calculating its scroll area.  Serializing every
+        # dock with saveState() from that sequence can fail fast in Qt5Core on
+        # Windows.  Dock moves/visibility changes and orderly shutdown still
+        # persist the layout; ordinary content relayouts must not.
 
     def showEvent(self, event):
         """ Have any child windows follow main-window state """
diff --git a/src/windows/verticalization.py b/src/windows/verticalization.py
index 2115d6b52..bd3491071 100644
--- a/src/windows/verticalization.py
+++ b/src/windows/verticalization.py
@@ -150,11 +150,6 @@ class VerticalizationDockContent(QWidget):
         self.sections = {}
         for key, title, description in self.SECTIONS:
             section = CollapsibleSection(_(title))
-            # Keeping every section expanded makes Qt repeatedly relayout a
-            # very tall dock while its scroll bar is dragged.  On this Windows
-            # build that can end in a native Qt shutdown, so expose each tool
-            # on demand instead.
-            section.toggle_button.setChecked(False)
             placeholder = QLabel(_(description), section.content)
             placeholder.setObjectName("verticalizationPlaceholder")
             placeholder.setWordWrap(True)
-- 
2.55.0.windows.3


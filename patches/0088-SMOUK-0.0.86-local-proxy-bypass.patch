diff --git a/scripts/run_smouk.py b/scripts/run_smouk.py
--- a/scripts/run_smouk.py
+++ b/scripts/run_smouk.py
@@
 import os
 import sys
 
+# Corporate proxy discovery can leave loopback traffic routed through the
+# proxy. OpenShot's thumbnail server is intentionally local, so make the
+# loopback exclusions explicit for both requests and urllib on Windows.
+_loopback_hosts = ("localhost", "127.0.0.1", "::1")
+for _proxy_bypass_name in ("NO_PROXY", "no_proxy"):
+    _existing = os.environ.get(_proxy_bypass_name, "")
+    _entries = [entry.strip() for entry in _existing.split(",") if entry.strip()]
+    _known = {entry.casefold() for entry in _entries}
+    for _host in _loopback_hosts:
+        if _host.casefold() not in _known:
+            _entries.append(_host)
+    os.environ[_proxy_bypass_name] = ",".join(_entries)

diff --git a/openshot-qt/src/windows/main_window.py b/openshot-qt/src/windows/main_window.py
--- a/openshot-qt/src/windows/main_window.py
+++ b/openshot-qt/src/windows/main_window.py
@@
-SMOUK_VERSION = "0.0.85"
+SMOUK_VERSION = "0.0.86"
